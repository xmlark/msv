
The "lib" directory contains third party libraries which are
1) used to build MSV and related projects
2) bundled in the release package for the user's convenience


The "regexp" directory contains the regular expression library
taken from Apache Xerces project. We should occasionally check
the update to their repository and copy them into this directory.

